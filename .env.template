# Photo HQ Backend Configuration
# Copy this file to .env and fill in your deployment values

# API Gateway endpoint URL
# Get from: aws cloudformation describe-stacks --stack-name photo-hq-dev --query 'Stacks[0].Outputs[?OutputKey==`ApiEndpoint`].OutputValue' --output text
API_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# Cognito User Pool ID
# Get from: aws cloudformation describe-stacks --stack-name photo-hq-dev --query 'Stacks[0].Outputs[?OutputKey==`UserPoolId`].OutputValue' --output text
USER_POOL_ID=us-east-1_XXXXXXXXX

# Cognito User Pool Client ID
# Get from: aws cloudformation describe-stacks --stack-name photo-hq-dev --query 'Stacks[0].Outputs[?OutputKey==`UserPoolClientId`].OutputValue' --output text
USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# AWS Region (default: us-east-1)
AWS_REGION=us-east-1

# Optional: Pre-existing access token (if you already have one)
# ACCESS_TOKEN=your-jwt-token-here
